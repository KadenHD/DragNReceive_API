import { initialize } from "./index.js";
import { } from './unlog.js';

const unlogSpec = async (browser, url, delay) => {
    const page = await initialize(browser, url);
    //
};

export default unlogSpec;