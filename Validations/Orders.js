const sadmin = "1";
const admin = "2";
const partner = "3";
const client = "4";

export const canCreateOrder = (currentUser, ticket) => {
    return (
        null
    );
}

export const canViewOrder = (currentUser, ticket) => {
    return (
        null
    );
}

export const canUpdateOrder = (currentUser, ticket) => {
    return (
        null
    );
}